* Context
The source of truth is the original EPUB export of a simplified version of "I promessi sposi". We split it into short LingQ-ready lessons using subsections marked by H3 headers like "N.M Title". The lesson generator lives in the `lesson-material/` uv project; the static site builder lives in the separate `website/` uv project.

* Done
- [X] Detect chapters via H2 "Capitolo N"
- [X] Detect subsections via H3 "N.M Title"
- [X] Split into per-subsection lessons
- [X] Produce minimal HTML output (UTF-8, headings preserved)
- [X] Produce plain-text output (paragraph breaks preserved)
- [X] Produce standalone EPUB output per lesson with embedded local images
- [X] Rewrite relative image paths when output dir differs
- [X] Generate EPUB, HTML, and TXT collections with whole-book, chapter, and section files
- [X] Split the repo into separate `lesson-material/` and `website/` uv projects with console entry points
- [X] Keep upstream-derived source and generated output under ignored `cc-by-nc-4.0-derivative-works/`
- [X] Strip inline English glosses by default, with an opt-out flag
- [X] Include section titles in section filenames
- [X] Strip source `FILE AUDIO` links from generated products

* Next Steps (High-Value)
- [ ] Length-based splitting: add `--max-words` or `--max-chars` to further split long subsections
- [ ] Link sanitization: strip tracking query params (`?q=...&ust=...&usg=...`) in external anchors
- [ ] Batch packaging: add `--zip OUT.zip` to bundle all lessons for easier manual import
- [ ] Filename templating: `--name-template "Cap-{chapter:02d}-{section:02d}-{slug}.html"`
- [ ] Title templating: allow custom title format for lesson headings
- [ ] Decide whether/how/where to rehost source audio files

* Robustness / QA
- [ ] Validate section counts per chapter against `nav.xhtml`; warn on missing/extra sections
- [ ] Optionally include chapter intro blocks that precede the first H3 (if present)
- [ ] Unicode normalization (NFC) pass to avoid import quirks
- [ ] Basic unit tests for parser (chapter detection, section detection, resource rewriting)

* LingQ Workflow Improvements
- [X] Provide a Justfile targets: `just lessons-epub`, `just lessons-html`, `just lessons-txt`, `just check-epub`
- [ ] Optional metadata CSV export (Title, Tags) to accompany lessons (for manual reference)
- [ ] Option to embed a top-of-lesson source note (e.g., chapter name, original link) via flag

* Code Hygiene / Docs
- [X] Add `--help` and examples to the script
- [ ] Expand README/AGENT_CONTEXT with LingQ tips and import screenshots (optional)
- [ ] Add a small CHANGELOG in the repo root

* Cleanup (Optional)
- [ ] Remove early test outputs: `GoogleDoc/chapters/` and `GoogleDoc/Capitolo-*.xhtml`
