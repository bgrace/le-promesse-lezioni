# Promessi Website

This uv project builds the static GitHub Pages site for the generated lesson artifacts.

The site builder uses Python's lightweight `string.Template` engine with templates and CSS stored
under `promessi_site/resources/`.
